<div class="album py-5 bg-body-tertiary">
  <div class="container">
    <h1 class="mt-5"><%- props.title %></h1>
    <p class="lead">Pin a footer to the bottom of the viewport in desktop browsers with this custom HTML and CSS. A
      fixed
      navbar has been added with <code class="small">padding-top: 60px;</code> on the <code
        class="small">main &gt; .container</code>.</p>
    <p>Back to <a href="/">Home Page</a> to view other features.</p>
  </div>
</div>